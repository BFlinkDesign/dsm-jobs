{
  "name": "dsm-jobs-css-tools",
  "private": true,
  "description": "Dev-only CSS toolchain: validate/transpile/minify the PWA's inline CSS with Lightning CSS, lint with stylelint.",
  "version": "1.0.0",
  "dependencies": {
    "lightningcss-cli": "^1.32.0",
    "stylelint": "^17.13.0",
    "stylelint-config-standard": "^40.0.0"
  }
}
