"""Supabase portal: scanner-side push (transport in push.py).

Browser-side auth/sync lives in the generated PWA (find_admin_jobs.APP_TEMPLATE);
the schema + operator runbook live in schema.sql / README.md next to this file.
"""
